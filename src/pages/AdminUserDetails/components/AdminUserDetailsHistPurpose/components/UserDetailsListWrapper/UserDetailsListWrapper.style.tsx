import { Wrapper } from '@constants/styles'
import { styled } from 'styled-components'

export const StyledUserDetailsWrapper = styled(Wrapper)`
	justify-content: start;
	width: 100%;
	align-items: start;
`
