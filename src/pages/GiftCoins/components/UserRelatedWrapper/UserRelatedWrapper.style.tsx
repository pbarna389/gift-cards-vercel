import { Wrapper } from '@constants/styles'
import { styled } from 'styled-components'

export const UserWrapper = styled(Wrapper)`
	align-items: flex-start;
	padding: 0.5rem;
	border: 1px solid lime;
	margin-top: 0.5rem;
`

export const UserCardWrapper = styled(Wrapper)`
	flex-direction: row;
	margin-top: 0.5rem;
`
